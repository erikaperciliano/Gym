<div class="item">
        <div>Avartar URL</div>
        <div>
        <input type="url" name="avatar_url" placeholder="http://" value="{{instructor.avatar_url}}">
        </div>
    </div>

    <div class="item">
        <div>Instructor</div>
        <div>
            <input type="text" name="name" placeholder="Instructor Name´s" value="{{instructor.name}}">
        </div>
    </div>

    <div class="item">
        <div>Bithrday</div>
        <div>
        <input type="date", name="birth" value"{{instructor.birth}}">
        </div>
    </div>

    <div class="item">
        <div>Gender</div>
        <div>
        <span>
            <input type="radio" name="gender" value="M"
                {% if instructor.gender == 'M' %}
                    checked
                {% endif %}> Male 
        </span>
        <span>
            <input type="radio" name="gender" value="F"
                {% if instructor.gender == 'F' %}
                    checked
                {% endif %}
            > Female 
        </span>
        </div>
    </div>

    <div class="item">
        <div>Action area </div>
        <div>
        <input type="text" name="services" placeholder="Prepare the services provided by commas" value="{{instructor.services}}">
        </div>
    </div>
    <button type="submit">Save</button>


